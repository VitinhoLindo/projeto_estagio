const BaseController = require('../../../lib/http/BaseController');

class MainController extends BaseController {
  constructor(request, response) { super(request, response); }
}

module.exports = MainController;